# Exact executable contract: robust line recovery

Run `python solution.py INPUT.json OUTPUT.json` from `submission/`. The input contains two-dimensional
points, including possible high-leverage outliers. Write numeric `slope` and `intercept` fields to
the output JSON. Hidden cases include exact, fractional, and adversarial-outlier configurations.

Also provide `reproduce.sh` and `REPORT.md` exactly as described in `TASK.md`. Malformed or missing
output fails closed. This synthetic capsule validates the framework lifecycle independently of the
visual research suite.
